{
  "name": "ai-tryon-lambda",
  "version": "1.0.0",
  "description": "AI Try-On Lambda (serverless backend)",
  "type": "module",
  "main": "lambda/handler.js",
  "license": "MIT",
  "scripts": {
    "build": "echo 'Lambda built via Docker, not via npm'",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.679.0",
    "@aws-sdk/s3-request-presigner": "^3.679.0",
    "@google/generative-ai": "^0.21.0",
    "openai": "^4.73.0",
    "sharp": "^0.33.5"
  }
}
